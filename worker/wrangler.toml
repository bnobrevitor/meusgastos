name = "saldo-bank-sync"
main = "src/index.js"
compatibility_date = "2026-07-01"

# Segredos (nunca ficam aqui, nunca em texto no repositório):
#   wrangler secret put PLUGGY_CLIENT_ID
#   wrangler secret put PLUGGY_CLIENT_SECRET
#   wrangler secret put SHARED_TOKEN
#   wrangler secret put PLUGGY_ITEM_IDS   (Item IDs do Meu Pluggy, separados por vírgula)
